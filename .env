# Docker Compose file selection
# Override with: COMPOSE_FILE=docker-compose.base.yml:docker-compose.prod.yml make ...
# or set in shell before running docker compose directly
COMPOSE_FILE=docker-compose.base.yml:docker-compose.dev.yml
